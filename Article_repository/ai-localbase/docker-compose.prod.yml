services:
  qdrant:
    image: qdrant/qdrant:v1.13.4
    container_name: qdrant
    ports:
      - "${QDRANT_BIND_ADDRESS:-127.0.0.1}:${QDRANT_HTTP_PORT:-6333}:6333"
      - "${QDRANT_BIND_ADDRESS:-127.0.0.1}:${QDRANT_GRPC_PORT:-6334}:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      - AI_LOCALBASE_QDRANT_API_KEY=${QDRANT_API_KEY:-}
    entrypoint:
      - /bin/sh
      - -c
      - |
        if [ -n "$$AI_LOCALBASE_QDRANT_API_KEY" ]; then
          export QDRANT__SERVICE__API_KEY="$$AI_LOCALBASE_QDRANT_API_KEY"
        fi
        exec ./entrypoint.sh
    healthcheck:
      test: ["CMD-SHELL", "if [ -n \"$${AI_LOCALBASE_QDRANT_API_KEY}\" ]; then curl -f -H \"api-key: $${AI_LOCALBASE_QDRANT_API_KEY}\" http://localhost:6333/collections; else curl -f http://localhost:6333/collections; fi"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    image: ghcr.io/veyliss/ai-localbase-backend:latest
    container_name: ai-localbase-backend
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    depends_on:
      qdrant:
        condition: service_healthy
    environment:
      - PORT=${PORT:-8080}
      - UPLOAD_DIR=${UPLOAD_DIR:-/app/data/uploads}
      - MAX_UPLOAD_BYTES=${MAX_UPLOAD_BYTES:-26214400}
      - STATE_FILE=${STATE_FILE:-/app/data/app-state.json}
      - CHAT_HISTORY_FILE=${CHAT_HISTORY_FILE:-/app/data/chat-history.db}
      - QDRANT_URL=${QDRANT_URL:-http://qdrant:6333}
      - QDRANT_API_KEY=${QDRANT_API_KEY:-}
      - QDRANT_COLLECTION_PREFIX=${QDRANT_COLLECTION_PREFIX:-kb_}
      - QDRANT_VECTOR_SIZE=${QDRANT_VECTOR_SIZE:-1024}
      - QDRANT_DISTANCE=${QDRANT_DISTANCE:-Cosine}
      - QDRANT_TIMEOUT_SECONDS=${QDRANT_TIMEOUT_SECONDS:-5}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      - CROSS_ENCODER_URL=${CROSS_ENCODER_URL:-}
      - CROSS_ENCODER_MODEL=${CROSS_ENCODER_MODEL:-BAAI/bge-reranker-v2-m3}
      - CROSS_ENCODER_API_KEY=${CROSS_ENCODER_API_KEY:-}
      - SPARSE_ENCODER_URL=${SPARSE_ENCODER_URL:-}
      - SPARSE_ENCODER_MODEL=${SPARSE_ENCODER_MODEL:-BAAI/bge-m3}
      - SPARSE_ENCODER_API_KEY=${SPARSE_ENCODER_API_KEY:-}
      - ENABLE_AUTH=${ENABLE_AUTH:-false}
      - AUTH_USERNAME=${AUTH_USERNAME:-root}
      - AUTH_PASSWORD=${AUTH_PASSWORD:-}
      - AUTH_SETUP_TOKEN=${AUTH_SETUP_TOKEN:-}
      - AUTH_RESET_TOKEN=${AUTH_RESET_TOKEN:-}
      - AUTH_RESET_PASSWORD=${AUTH_RESET_PASSWORD:-}
      - JWT_SECRET=${JWT_SECRET:-}
      - ENABLE_HYBRID_SEARCH=${ENABLE_HYBRID_SEARCH:-true}
      - ENABLE_QUERY_TRANSLATION=${ENABLE_QUERY_TRANSLATION:-true}
      - QUERY_TRANSLATION_MODE=${QUERY_TRANSLATION_MODE:-mt}
      - QUERY_TRANSLATION_PROVIDER=${QUERY_TRANSLATION_PROVIDER:-}
      - QUERY_TRANSLATION_BASE_URL=${QUERY_TRANSLATION_BASE_URL:-http://host.docker.internal:8090}
      - QUERY_TRANSLATION_API_KEY=${QUERY_TRANSLATION_API_KEY:-}
      - QUERY_TRANSLATION_MODEL=${QUERY_TRANSLATION_MODEL:-}
      - QUERY_TRANSLATION_TIMEOUT_SECONDS=${QUERY_TRANSLATION_TIMEOUT_SECONDS:-20}
      - ENABLE_SEMANTIC_RERANKER=${ENABLE_SEMANTIC_RERANKER:-true}
      - ENABLE_QUERY_REWRITE=${ENABLE_QUERY_REWRITE:-false}
      - ENABLE_SEMANTIC_CACHE=${ENABLE_SEMANTIC_CACHE:-false}
      - ENABLE_CONTEXT_COMPRESSION=${ENABLE_CONTEXT_COMPRESSION:-false}
      - RETRIEVAL_TOPK_DOCUMENT=${RETRIEVAL_TOPK_DOCUMENT:-6}
      - RETRIEVAL_CANDIDATE_TOPK_DOCUMENT=${RETRIEVAL_CANDIDATE_TOPK_DOCUMENT:-12}
      - RETRIEVAL_TOPK_KNOWLEDGE_BASE=${RETRIEVAL_TOPK_KNOWLEDGE_BASE:-10}
      - RETRIEVAL_CANDIDATE_TOPK_ALL_DOCS=${RETRIEVAL_CANDIDATE_TOPK_ALL_DOCS:-32}
      - RETRIEVAL_MAX_CHUNKS_PER_DOCUMENT=${RETRIEVAL_MAX_CHUNKS_PER_DOCUMENT:-2}
      - RETRIEVAL_MAX_CONTEXT_CHARS=${RETRIEVAL_MAX_CONTEXT_CHARS:-2400}
      - RETRIEVAL_ENABLE_AUTO_EXPAND=${RETRIEVAL_ENABLE_AUTO_EXPAND:-false}
      - ENABLE_MCP=${ENABLE_MCP:-false}
      - ENABLE_MCP_LEGACY_TOKEN=${ENABLE_MCP_LEGACY_TOKEN:-false}
      - MCP_BASE_PATH=${MCP_BASE_PATH:-/mcp}
      - MCP_REQUEST_TIMEOUT_SECONDS=${MCP_REQUEST_TIMEOUT_SECONDS:-15}
      - MCP_REQUESTS_PER_MINUTE=${MCP_REQUESTS_PER_MINUTE:-120}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backend/data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:${PORT:-8080}/health"]
      interval: 10s
      timeout: 3s
      retries: 3

  frontend:
    image: ghcr.io/veyliss/ai-localbase-frontend:latest
    container_name: ai-localbase-frontend
    ports:
      - "${FRONTEND_PORT:-4173}:4173"
    depends_on:
      backend:
        condition: service_healthy
    environment:
      - BACKEND_URL=http://backend:${PORT:-8080}
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://localhost:8080}
      - NGINX_CLIENT_MAX_BODY_SIZE=${NGINX_CLIENT_MAX_BODY_SIZE:-32m}
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  qdrant_storage:
